<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' type="text/css" href='/bootstrap/css/bootstrap.css' />
    <script type="text/javascript" src="/bootstrap/js/jquery-3.5.1.min.js"></script>
    <script type="text/javascript" src="/bootstrap/js/bootstrap.js"></script>
  </head>
  <body>
    <button><a href="/article">发表文章</a></button>
    <button><a href="/logout">退出</a></button>
    <h1><%= title %></h1>
    <p>Welcome <%= currentUser %></p>

    <table class="table">
      <caption>Optional table caption.</caption>
      <thead>
        <tr>
          <th>title</th>
          <th>作者</th>
          <th>日期</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
        <%for(let i=0; i < list.length; i++){%>
          <tr>
            <th scope="row"><a href="/detail/<%= list[i]._id%>"><%= list[i].title%></a></th>
            <td><%= list[i].author%></td>
            <td><%= list[i].createData%></td>
            <td>
              <a class="btn btn-primary" role='button' href="/article/update/<%= list[i]._id%>">更新</a>
              <a class="btn btn-danger" href="/article/delete/<%= list[i]._id%>" role='button' >删除</a>
            </td>
          </tr>
        <%}%>
      </tbody>
    </table>
  </body>
</html>
